import { WorkOrder, Customer } from './types';

// Placeholder empty datasets. Replace by fetching from a backend or IPC later.
export const workOrders: WorkOrder[] = [];
export const customers: Customer[] = [];

// Deprecated seed helpers removed intentionally while real data integration is pending.
