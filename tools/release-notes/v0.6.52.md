## v0.6.52

- Fixed the false **shop session is not ready** error shown to authenticated users when retrying queued client emails.
- Client Update History now resolves the shop using the established active staff-profile status.
- Added regression coverage for the authenticated retry lookup.

Web Interface: https://mattstechwisdom.github.io/GB-POS
