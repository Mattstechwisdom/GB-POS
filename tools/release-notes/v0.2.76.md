Release 0.2.76

Changes:
- Fixed packaged update detection to scan published GitHub releases and choose the highest real version instead of relying on GitHub's stale releases/latest pointer.
- Applied the release lookup fix in both the renderer fallback path and the main Electron update check so installed builds detect newer published versions consistently.
- Published specifically to verify update prompting from older installed machines.