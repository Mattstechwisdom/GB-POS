## v0.6.38

- Adds an `All Parts / By Device` inventory view organized as Device → Part Category → Parent Part → Variant.
- Reuses compatible-device assignments so one stock record can be found under every supported model without duplicate inventory.
- Lets Durant Media record a product/supplier URL separately from the invoice URL for each line item.
- Replaces the raw Durant proposal display with a responsive, styled review window containing clickable links and clear approval controls.

Web Interface: https://mattstechwisdom.github.io/GB-POS
