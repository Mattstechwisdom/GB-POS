Release 0.2.72

Changes:
- Reduced startup drag by making the update gate non-blocking and adding hard timeouts around the GitHub latest-release checks.
- Kept packaged update detection tied to GitHub releases, with direct installer download and relaunch fallback still available when the provider path fails.
- Stopped work order and sale item editors from popping open during normal selection so item entry stays faster and only opens Edit from the context menu.