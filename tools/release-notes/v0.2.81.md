## GadgetBoy POS v0.2.81

- update prompt now checks GitHub tags directly so newer versions are detected even when the releases API lags behind
- startup now shows a dedicated GitHub update check screen instead of silently falling through to the home page during the check
- if a newer GitHub tag exists before installer assets finish publishing, the app will still notify you and open Releases instead of pretending no update exists