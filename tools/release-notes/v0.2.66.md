Release 0.2.66

Changes:
- Auto-updater: explicitly configures the GitHub feed instead of relying only on inferred publish metadata.
- Auto-updater: falls back to the GitHub latest release API if the updater provider does not return a latest version.
- Auto-updater: adds better startup logging for current/latest version detection.
