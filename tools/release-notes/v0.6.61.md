# GadgetBoy POS v0.6.61

This reliability release fixes missing client search results, intermittent interface stalls, duplicate work orders during checkout, blocked form fields after deletion, and print layouts that pushed terms and signatures onto an unnecessary page.

Normal saves and deletes are now committed locally first and synchronized with Supabase through the durable retry queue, keeping the POS responsive without sacrificing cross-device synchronization.
