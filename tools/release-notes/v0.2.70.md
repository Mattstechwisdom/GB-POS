Release 0.2.70

Changes:
- Reduced extra work-order and sale editor re-renders by narrowing which panels repaint during typing.
- Added a direct GitHub latest-release fallback in the startup update gate so future installed builds can still detect updates even if the provider check misbehaves.
- Kept release publishing strict so each tag only ships its own installer assets.