Release 0.2.71

Changes:
- Fixed the EOD technician summary declaration order so the renderer type-check passes again.
- Kept startup update detection tied to the latest GitHub release so newer packaged builds are recognized immediately.
- Added a direct GitHub installer download fallback for Update Now so packaged installs can still download, install, relaunch, and reopen when the provider-based updater path fails.