Release 0.2.68

Changes:
- Split the renderer into per-window chunks so child windows open faster instead of loading the full app bundle every time.
- Reduced autosave overhead in large forms so typing in work orders and sales feels more responsive.
- Avoided redundant totals state updates in heavy editor windows to cut extra re-renders during data entry.