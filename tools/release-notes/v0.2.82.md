## GadgetBoy POS v0.2.82

- work orders only move to the top of the main list for transactional changes or item changes
- opening an existing work order no longer autosaves the unchanged record on load, so reprinting a receipt will not bump the ticket
- published so 0.2.81 can test the GitHub tag-based update prompt against a newer version