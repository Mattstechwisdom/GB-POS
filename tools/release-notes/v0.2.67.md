Release 0.2.67

Changes:
- Update check at launch now uses GitHub latest release as the primary source of truth.
- Update prompt now appears when a newer GitHub release exists, even if the provider check returns a warning.
- Update gate precedence fixed so an available update is shown instead of silently continuing.
