# GadgetBoy POS v0.6.93

- Work-order release forms now wait until their QR image is generated, rendered, and decoded before printing.
- Temporary QR-service delays are retried three times.
- Printing pauses with a visible error rather than producing a form without its required QR code.
- The release pipeline now runs the real Electron QR-print timing test.
