Release 0.2.75

Changes:
- Simplified EOD batch emails so the generated preview is the exact message body that gets sent, avoiding duplicate summary content.
- Fixed customer history sales matching so older sales still appear under the correct customer when legacy records are missing a direct customerId link.
- Reworked saved quotes into monthly sections and only move a quote into the current month when quote content actually changes, not when it is merely opened and autosaved unchanged.
- Published specifically to validate packaged update detection against a newer GitHub release feed.