# GadgetBoy POS v0.6.81

- Restores the previously working native electron-updater install-and-relaunch flow.
- Removes the competing PowerShell retry installer introduced in v0.6.80, which could race the native NSIS updater and report that old files could not be uninstalled.
- Keeps the silent one-click update, pre-install data flush, window cleanup, and automatic relaunch behavior.
