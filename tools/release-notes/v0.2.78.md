Release 0.2.78

Changes:
- Hardened startup update detection so the renderer always cross-checks the GitHub releases API and prefers the highest real version, even if the packaged main-process updater reports no update.
- This specifically fixes false negatives where the app finishes its startup loading flow without showing the available update prompt.
- Published to verify update prompting from the manually installed 0.2.77 build.