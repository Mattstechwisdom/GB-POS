## GadgetBoy POS v0.2.80

- updater checks now use a live GitHub releases query as the source of truth instead of local/provider metadata
- updater only treats a release as available after GitHub shows published Windows installer assets
- updater download now prefers the GitHub installer directly so prompt-and-install testing follows the same published release path
- checkout receipt printing stays silent for both work orders and sales without showing the preview popup